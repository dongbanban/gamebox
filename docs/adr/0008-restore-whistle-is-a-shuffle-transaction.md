# 复原哨只恢复最近一次安全乱序事务

「狗了个狗」继续遵守 ADR-0001 的单向局内状态规则。复原哨不是通用撤销：它只在安全乱序实际改变槽序后保存一个短生命周期事务快照，并在下一次其他玩家动作前恢复乱序前的完整暂存槽条目、机制进度与乱序相关钥匙掉落状态。

复原时不回滚触发乱序前的普通选择，也不返还复原哨次数。触发乱序的方块保留在暂存槽并转为普通方块，避免再次触发。稳定原序、终局、其他棋盘选择、其他道具、道具组变更或离开关卡都会使快照失效；不保存跨关卡或跨刷新历史。
