# 游戏道具组配置跨会话保留，局内状态不保留

每个游戏按自身规则首次确定的道具类型选择作为道具组配置，跨刷新、离开与回访保留；「狗了个狗」固定选择 3 种不同道具。道具次数、棋盘与暂存槽等局内状态不保留。活动关卡中确认变更道具组会重置当前局但保持同一关卡布局，失败结果页变更用于重试当前关卡，通关结果页变更用于下一关，且不回滚已确认的通关、奖励或解锁；这样既保留长期选择，又避免持久化复杂半局状态与进度回滚。
