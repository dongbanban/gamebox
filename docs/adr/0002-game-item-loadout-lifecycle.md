# 游戏道具组配置跨会话保留，局内状态不保留

每个游戏按自身规则首次确定的道具类型选择作为道具组配置，跨刷新、离开与回访保留；「狗了个狗」固定选择 3 种不同道具，候选集合为 7 种。道具次数、棋盘与暂存槽等局内状态不保留。道具组确认即开始本关，进行中的挑战不允许换组；准备态可以确认初始组，失败或通关结果页可以显式更换。失败结果页换组用于重试当前关卡并沿用原棋盘与 `runSeed`，通关结果页换组用于下一关并生成新尝试；均不回滚已确认的通关、奖励或解锁。某一关没有锁槽或没有合法目标不使保存的道具组失效，也不触发静默替换；这样既保留长期选择，又避免持久化复杂半局状态与进度回滚。
