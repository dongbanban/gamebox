# Gamebox Domain

Gamebox 是浏览器小游戏合集。领域语言覆盖玩家身份、游戏目录、关卡、棋盘与消除过程。

## 用户与游戏

**用户**：在当前游戏合集中的单一玩家身份。MVP 不区分真实账号与多个用户档案。
_避免_：账号、账户、登录用户

**游戏**：游戏合集中的一个可独立游玩的项目，拥有自己的规则、关卡与进度。
_避免_：玩法、模块

**游戏目录**：合集内可发现的游戏集合。每个游戏拥有稳定身份、名称与简介。
_避免_：游戏菜单、游戏卡片

**游戏进度**：用户在某个游戏中已经完成或解锁的关卡状态。局内未完成过程不属于游戏进度；积分属于通关奖励，不决定关卡解锁。
_避免_：存档、局内状态

**关卡**：游戏中一次完整的挑战。关卡按顺序解锁；「狗了个狗」采用可持续生成的无限关卡序列。
_避免_：回合、地图

## 棋盘与消除

**棋盘**：关卡中承载全部方块的可玩区域。
_避免_：牌桌、背景

**棋盘形状**：棋盘内部实际可放置方块的逻辑轮廓。当前所有关卡使用不规则逻辑轮廓；可见棋盘表面统一为完整长方形，轮廓外表面不承载方块。
_避免_：背景形状、主题形状

**方块**：棋盘中的可选择消除单位。方块具有图案类型与层叠位置。
_避免_：牌、卡片

**冻结方块**：可以像普通方块一样点击并进入暂存槽，但带有冻结视觉特效；冻结状态会随方块进入暂存槽，不参与三消。进入暂存槽后，每个冻结方块独立统计其后的其他成功三消，累计 2 次自动融化；火把与万能方块只是融化捷径，不是唯一解法。
_避免_：冰块、冻结牌

**图案类型**：决定方块是否可以互相组成三消的匹配类别。「狗了个狗」首发图案类型为：打工狗、单身狗、舔狗、看门狗、疯狗、拆家狗、龇牙狗、社恐狗、吃货狗、傻狗。
_避免_：颜色、外观

**层叠关系**：方块之间由上下层形成的遮挡关系。任意更高层活动方块与下方方块存在正面积重叠时，下方方块不可点击；仅接触边或角不算重叠。
_避免_：深度、堆高

**可点击方块**：当前没有更高层活动方块与其正面积重叠的方块。
_避免_：激活方块、前景方块

**暂存槽**：点击方块后的临时容纳区域，基础容量为 7 个位置。三个相同图案类型进入暂存槽后组成三消并移除；同次操作会持续处理所有完整三连。道具可在当前关卡增加容量，效果不跨关卡保留。
_避免_：卡槽、背包

**三消**：三个相同图案类型的方块从暂存槽中组成一组并被移除的行为，不要求三个方块在暂存槽中相邻。同一轮中存在多组时，全部完整三连都被处理。
_避免_：合成、匹配消除

**通关**：棋盘中没有剩余方块的关卡结果。
_避免_：胜利局、完成回合

**通关奖励**：用户首次完成关卡后获得的积分结果。「狗了个狗」按关卡难度发放；其他游戏可以拥有独立奖励规则。重复挑战已完成关卡不重复累计奖励，不作为进入下一关的条件。
_避免_：解锁分、门槛分

**失败**：暂存槽已满且当前操作无法触发消除的关卡结果。失败判定后不能使用道具救局。
_避免_：死亡、出局

**道具**：具体游戏在当前关卡内可使用的辅助能力；游戏也可以完全不提供道具。道具数量是关卡配额，由游戏或关卡规则初始化；每种道具独立计数，成功使用扣除 1 次，使用条件不满足不扣次数。使用只影响当前局，效果完成后立即重算局面；清空最后一个方块时立即通关。失败、重开或进入新关卡后按规则重新初始化。使用次数不写入公共游戏进度，不跨游戏或关卡累积，也不影响通关奖励、积分或关卡解锁。道具类型选择可以跨关卡顺延，但不等于次数累积。「狗了个狗」的道具永远需要用户主动触发，且道具效果不会使当前局失败。
_避免_：技能、库存道具

**道具集合**：具体游戏在当前关卡中提供给用户选择的道具类型集合。不同游戏可提供不同道具集合；「狗了个狗」使用固定道具集合，当前暂定 5 种。
_避免_：全局道具库、公共库存

**道具次数**：某种道具在当前关卡中可成功使用的次数。不同道具次数独立计算；「狗了个狗」每种道具每关为 1–2 次，具体值随关卡变化，同一关卡重开或变更道具组时保持不变。
_避免_：用户持有数量、累计库存

**道具目标**：道具使用时由具体游戏定义的作用对象，可以不存在，也可以是方块、暂存槽位置等局内对象。目标选择期间暂停普通方块与其他道具操作；目标无效或用户取消时使用失败，不执行效果，不扣道具次数。
_避免_：公共目标、全局选择器

**道具使用确认**：需要选择目标或图案的道具，在选择完成前可以取消使用；取消不扣次数、不改变局面，最终确认后才执行效果、扣除次数，并进入不可撤销的原子操作。无目标道具在效果说明已展示的前提下，点击即执行，不额外弹出确认。
_避免_：预扣次数、半执行

**道具三消移除**：以暂存槽中已有图案为目标，补齐该图案的完整三件后一次移除；暂存槽已有 1 个或 2 个时，分别由游戏从棋盘自动补 2 个或 1 个。补充方案先过滤掉会破坏无道具可解路径的方案，再优先取被遮挡方块；同级合法方案按关卡方块稳定顺序选择，不使用随机。无法满足时使用失败且不扣次数；成功时展示补齐并移除动画。
_避免_：单个图案删除、回收方块

**道具后可解局面**：道具效果完成后的当前棋盘与暂存槽状态。忽略剩余道具后，该状态仍需存在至少一条普通选择通关路径。
_避免_：依赖道具的可解、工具解法

**道具可用状态**：道具次数大于 0 且当前局面存在合法目标或合法效果时的状态。没有合法目标、次数用尽或当前局已结束时，道具不可使用；局面变化后重新评估。
_避免_：按钮亮起、库存充足

**万能方块**：「狗了个狗」的道具。使用后从本关全部图案类型中选择一种，万能方块转化为该图案并进入暂存槽；它保留高亮边框，与普通方块区分。同一关卡可重复选择同一图案。转化、入槽、选定图案的棋盘补偿与三消结算属于一次原子操作；若会造成失败则不可使用。
_避免_：百搭牌、通配卡

**暂存槽容量提升**：道具使当前局暂存槽容量立即增加 1 格。「狗了个狗」每关最多使用一次；通关、失败或重开后失效。
_避免_：永久扩槽、全局容量

**当前道具组**：某个游戏按自身规则确定、用于当前关卡的道具类型选择。「狗了个狗」首次进入时从固定集合中选出 3 种不同道具；选满后需要显式确认，后续关卡默认顺延使用该组。当前道具组不携带已用次数，关卡重新开始时次数按规则初始化。
_避免_：装备栏、用户背包

**道具组配置**：某个游戏按自身规则独立持久保留的道具类型选择。「狗了个狗」保存 3 种不同道具；保存的道具失效时配置失效，需按游戏规则重新选择，不静默替换。道具组配置跨离开、刷新与回访保留；不包含道具次数、棋盘或暂存槽等半局状态，重置本地数据时清除。
_避免_：道具库存、局内存档

**道具组变更**：用户可按具体游戏规则在任意稳定状态主动替换当前道具组；变更页默认预选旧组，警告确认只进入编辑态，最终确认新组后才在同一关卡布局上重置当前关卡的局内通关进度并应用新组。对「狗了个狗」，新组至少替换 1 种道具，新旧组完全相同视为取消，不重置当前局。道具效果动画或目标选择期间不可变更。活动关卡变更重置当前局，失败结果页变更用于重试当前关卡，通关结果页变更用于下一关；变更不回滚已确认的通关或关卡解锁。
_避免_：局内换组、临时库存调整

**撤销**：将已完成的普通选择、三消或道具效果回滚。「狗了个狗」永不支持撤销。
_避免_：回退、悔棋

**可解关卡**：至少存在一条不依赖道具的通关路径的关卡。局部区域中同一图案类型出现数量可以任意；整关同一图案类型总数仍为 3 的倍数。
_避免_：必胜关、无解关

**安全选择**：玩家当前可执行、且执行后仍保留至少一条无道具通关路径的选择。安全选择数量用于衡量关卡策略空间与难度。
_避免_：正确答案、提示步

**无限关卡**：关卡数量持续扩展的模式，不代表单局方块数量无限增长。方块数量从 90 起步，阶段增长至 180 后封顶；所有关卡使用不规则内部逻辑轮廓与完整长方形可见棋盘，后续难度由方块数量、层数、部分重叠布局与安全选择空间提升。
_避免_：随机局、无限棋盘
