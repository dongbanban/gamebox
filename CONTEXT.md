# Gamebox Domain

Gamebox 是浏览器小游戏合集。领域语言覆盖玩家身份、游戏目录、关卡、棋盘与消除过程。

## 用户与游戏

**用户**：在当前游戏合集中的单一玩家身份。MVP 不区分真实账号与多个用户档案。
_避免_：账号、账户、登录用户

**游戏**：游戏合集中的一个可独立游玩的项目，拥有自己的规则、关卡与进度。
_避免_：玩法、模块

**游戏目录**：合集内可发现的游戏集合。每个游戏拥有稳定身份、名称与简介。
_避免_：游戏菜单、游戏卡片

**游戏进度**：用户在某个游戏中已经完成或解锁的关卡状态。局内未完成过程不属于游戏进度；积分属于通关奖励，不决定关卡解锁。
_避免_：存档、局内状态

**关卡**：游戏中一次完整的挑战。关卡按顺序解锁；「狗了个狗」采用最多 99 关的可持续生成序列，第 1 关起不使用固有关卡快照，每次关卡尝试都按新的 `runSeed` 生成棋盘。
_避免_：回合、地图

**关卡尝试**：用户进入、重开或重试某一关的一次独立局面。每次尝试重新生成棋盘形状、方块位置与图案；启用的多种特殊机制及其方块分布、状态、锁槽数量也同步重新生成，幻化方块的伪装图案与真实图案也重新生成。本次尝试内生成结果保持不变；离开游戏或刷新后回访未完成关卡视为新尝试，不保存半局状态。`runSeed` 写入回放元数据，不写入公共游戏进度。随机候选必须通过所有已启用特殊机制的组合校验，并落在当前关卡的难度目标范围内，超出范围即重抽。生成允许显示短暂加载；下一关优先后台预生成，生成计算不阻塞主线程。
_避免_：随机回合、临时关卡

**特殊机制**：改变基础方块生成、可点击条件、入槽行为或三消规则的关卡规则扩展。关卡配置固定本关允许的特殊机制类型；一个关卡可同时启用多种特殊机制。v13 先按逻辑方块总数 `N` 计算 `floor(N × 0.30)` 逻辑机制预算，再按冻结/幻化/磁吸权重 1、双生权重 2 分配数量；1–5、6–15、16–30、31–99 分别约为 27、32–37、43–54、54 个逻辑单位，特殊方块物理数量随 `N` 提高。锁槽独立为 0–2。总特殊机制密度按逻辑方块单位校验，v13 目标贴近且不超过逻辑总数的 30%；锁槽不计入该密度。runSeed 只在已配置机制范围内随机数量分配、位置、图案、状态与锁槽数量。机制反馈按规则提供；幻化与双生棋盘视角与普通方块一致，不显示常驻特殊标记，但入槽揭示/分裂反馈保留。难度、无道具求解与预估时长必须模拟实际特殊机制。不同机制可以同关共存，但单个方块最多承载一种特殊机制。
_避免_：特殊道具、随机效果

**runSeed**：关卡尝试的随机种子。由系统在新尝试开始时生成，驱动该尝试的全部关卡随机结果；机制内部可以从它派生相互独立的可复现随机流。保存后可复现关卡、回放测试与 Bug。不可直接依赖未保存的运行时随机状态。
_避免_：用户种子、全局随机种子

## 棋盘与消除

**棋盘**：关卡中承载全部方块的可玩区域。
_避免_：牌桌、背景

**棋盘形状**：棋盘内部实际可放置方块的逻辑轮廓。当前所有关卡使用不规则逻辑轮廓；可见棋盘表面统一为完整长方形，轮廓外表面不承载方块。
_避免_：背景形状、主题形状

**方块**：棋盘中的可选择消除单位。方块具有图案类型与层叠位置。
_避免_：牌、卡片

**冻结方块**：可以像普通方块一样点击并进入暂存槽，但带有冻结视觉特效；冻结方块的出现、位置、图案与冻结状态按关卡尝试随机生成，本次尝试内保持不变。冻结状态会随方块进入暂存槽，不参与三消。进入暂存槽后，每个冻结方块独立统计其后的其他成功三消，累计 2 次自动融化；火把与万能方块只是融化捷径，不是唯一解法。
_避免_：冰块、冻结牌

**冻结关卡**：包含冻结方块的关卡。冻结关卡仍需存在无道具通关路径，不限制火把或万能方块是否进入道具组；两次其他三消可以使冻结方块自动融化。
_避免_：必带火把关卡、必带万能方块关卡

**幻化方块**：「狗了个狗」中只出现在棋盘上的特殊方块，棋盘使用普通方块样式但显示伪装图案；不显示 mask、fuzzy 或常驻特殊标记。伪装图案与真实图案按关卡尝试重新生成，本次尝试内保持不变。直接点击后进入飞入暂存槽的动画，方块在飞行过程中显现真实图案；飞入期间占用 1 个槽位，棋盘不可点击，动画结束后按真实图案进入暂存槽并按普通方块规则检查三消，随后棋盘恢复点击。不依赖检测仪。
_避免_：假方块、未知方块

**磁吸方块**：「狗了个狗」中出现在棋盘上的特殊方块，持续显示磁性识别动效。进入暂存槽时先占用自身槽位，再从非磁吸、真实图案不同的方块中随机吸取 1 个；优先吸取可点击方块，没有可点击候选时才吸取不可点击方块。没有合法目标时独自入槽；不可吸引其他磁吸方块，不产生连锁。被吸方块保留自身机制并按自身入槽规则与动效处理。吸引完成后磁性消耗，暂存槽内按普通方块参与三消。目标选择由本次关卡尝试的可复现随机结果决定。
_避免_：磁铁方块、连锁磁吸

**双生方块**：「狗了个狗」中出现在棋盘上的特殊方块，棋盘使用普通方块样式，不显示常驻双生识别动效。棋盘上占 1 个层叠对象；进入暂存槽后播放分裂反馈，生成 2 个独立同图案方块，各计 1 个逻辑方块单位，按普通同图案入槽顺序保持相邻。分裂后不保留双生状态；棋盘层叠与下层解锁仍按 1 个棋盘对象处理。每种图案的逻辑总数仍需为 3 的倍数。
_避免_：复制方块、双倍点击

**图案类型**：决定方块是否可以互相组成三消的匹配类别。「狗了个狗」首发图案类型为：打工狗、单身狗、舔狗、看门狗、疯狗、拆家狗、龇牙狗、社恐狗、吃货狗、傻狗。
_避免_：颜色、外观

**层叠关系**：方块之间由上下层形成的遮挡关系。任意更高层活动方块与下方方块存在正面积重叠时，下方方块不可点击；仅接触边或角不算重叠。
_避免_：深度、堆高

**可点击方块**：当前没有更高层活动方块与其正面积重叠的方块。
_避免_：激活方块、前景方块

**逻辑方块单位**：用于总方块数、图案配额、暂存槽压力、可解性与难度计算的方块计量。普通、冻结、幻化、磁吸方块各计 1 个单位；双生方块计 2 个单位；磁吸吸取的目标本来属于棋盘剩余单位，不重复计数。棋盘层叠仍按实际棋盘对象计算。
_避免_：视觉方块数、点击次数

**暂存槽**：点击方块后的临时容纳区域，基础总容量为 7 个位置。关卡尝试可有 0–2 个上锁槽；有效容量为总容量减去上锁槽数，空闲有效槽位为有效容量减去暂存槽当前逻辑单位数。暂存槽始终按点击顺序追加方块；连续相邻的三个相同图案类型方块组成三消并移除，同次操作会持续处理所有连续区段中的完整三连。扩容道具使总容量与有效容量各增加 1，不改变上锁槽数；效果不跨关卡保留。
_避免_：卡槽、背包

**锁槽**：暂存槽中由关卡尝试随机生成的不可用位置。数量为 0–2 个，本次尝试内保持不变；上锁槽位于暂存槽右侧连续区域并持续显示锁定视觉。钥匙可逐格解锁；无钥匙时锁槽永久限制无道具可解路径。
_避免_：上锁方块、永久锁

**三消**：暂存槽中连续相邻的三个相同图案类型方块组成一组并被移除的行为；非相邻同类方块不跨位置组成三消。同一轮中存在多组或连续区段包含多组三连时，全部完整三连都被处理。
_避免_：合成、匹配消除

**通关**：棋盘中没有剩余方块的关卡结果。
_避免_：胜利局、完成回合

**通关奖励**：用户首次完成关卡后获得的积分结果。「狗了个狗」沿用当前奖励公式，根据本次生成结果的难度指标计算；其他游戏可以拥有独立奖励规则。重复挑战已完成关卡不重复累计奖励，不作为进入下一关的条件。
_避免_：解锁分、门槛分

**失败**：一次原子入槽/分裂/吸引结算完成后，暂存槽超过有效容量，或暂存槽达到有效容量且当前操作无法触发消除的关卡结果。双生导致的超容量与磁吸带来的满槽无解均属于玩家操作结果，不做安全拦截。失败判定后不能使用道具救局。
_避免_：死亡、出局

**道具**：具体游戏在当前关卡内可使用的辅助能力；游戏也可以完全不提供道具。道具数量是关卡配额，由游戏或关卡规则初始化；每种道具独立计数，成功使用扣除 1 次，使用条件不满足不扣次数。使用只影响当前局，效果完成后立即重算局面；清空最后一个方块时立即通关。每个道具必须有对应的使用视觉效果，不要求额外文字状态提示。失败、重开或进入新关卡后按规则重新初始化。使用次数不写入公共游戏进度，不跨游戏或关卡累积，也不影响通关奖励、积分或关卡解锁。道具类型选择可以跨关卡顺延，但不等于次数累积。「狗了个狗」的道具永远需要用户主动触发，且道具效果不会使当前局失败。钥匙属于常规道具，但已选入时初始次数为 0，由成功三消掉落增加次数。
_避免_：技能、库存道具

**道具集合**：具体游戏在当前关卡中提供给用户选择的道具类型集合。不同游戏可提供不同道具集合；「狗了个狗」使用固定 7 种道具集合：道具三消移除、暂存槽容量提升、万能方块、火把、检测仪、消磁仪、钥匙。
_避免_：全局道具库、公共库存

**道具次数**：某种道具在当前关卡中可成功使用的次数。不同道具次数独立计算；v13 中除钥匙外每种「狗了个狗」道具每关最多成功使用 1 次，取消、无效目标与失败不扣次。钥匙是例外：已选入时初始次数为 0，按既定成功三消掉落规则增加，累计不超过本局锁槽数。同一关卡重开、重试或变更道具组时按规则重新初始化。
_避免_：用户持有数量、累计库存

**道具目标**：道具使用时由具体游戏定义的作用对象，可以不存在，也可以是方块、暂存槽位置等局内对象。棋盘方块型目标必须同时满足当前可点击条件；火把、检测仪、消磁仪不得把不可点击方块作为目标，道具三消移除的棋盘自动补充候选也只能取当前可点击的普通方块。暂存槽目标按道具专属规则判断，不套用棋盘可点击条件。目标选择期间暂停普通方块与其他道具操作，合法目标高亮，已填充的非目标暂存槽方块与非目标棋盘方块统一置灰；目标无效或用户取消时使用失败，不执行效果，不扣道具次数。
_避免_：公共目标、全局选择器

**道具使用确认**：需要选择目标或图案的道具，在选择完成前可以取消使用；取消不扣次数、不改变局面，最终确认后才执行效果、扣除次数，并进入不可撤销的原子操作。无目标道具在效果说明已展示的前提下，点击即执行，不额外弹出确认。
_避免_：预扣次数、半执行

**道具三消移除**：以暂存槽中两个相邻、同图案、无特殊机制的方块为目标，从当前可点击的棋盘普通方块中自动补 1 个后一次移除。不存在合法方块对、补充候选或道具后可解方案时不可用；成功时展示补充一个方块并移除动画，不支持槽内一个方块自动补两个。
_避免_：单个图案删除、回收方块

**道具后可解局面**：道具效果完成后的当前棋盘与暂存槽状态。忽略剩余道具后，该状态仍需存在至少一条普通选择通关路径。
_避免_：依赖道具的可解、工具解法

**道具可用状态**：道具次数大于 0 且当前局面存在合法目标或合法效果时的状态。没有合法目标、次数用尽或当前局已结束时，道具不可使用；局面变化后重新评估。
_避免_：按钮亮起、库存充足

**万能方块**：「狗了个狗」的道具。使用时点击暂存槽内一个已有方块作为目标，万能方块复制其图案并严格追加到暂存槽末尾；不单独列出本关图案。只有当前暂存槽中已有且存在合法使用计划的图案可选，没有合法槽内目标时不可用。同一关卡可重复选择同一图案。万能保留高亮边框，与普通方块区分。转化、入槽、目标图案的棋盘补偿与三消结算属于一次原子操作；仅追加前槽尾两个连续同图案方块可与万能组成三消，其中冻结方块可直接作为本次三消参与者，不跨过其他槽位重排；若会造成失败则不可使用。
_避免_：百搭牌、通配卡

**火把**：「狗了个狗」的主动道具，可作用于当前可点击的棋盘冻结方块或暂存槽冻结方块。棋盘方块融化后留在原位，暂存槽方块融化后立即重新判断三消；融化后的方块就是普通方块。
_避免_：解冻器、融化卡

**检测仪**：「狗了个狗」的主动道具，只能作用于当前可点击的棋盘幻化方块。确认使用后锁定棋盘，目标原地播放显现动画；动画结束后还原为普通方块，棋盘恢复点击。不占用暂存槽，不触发三消。
_避免_：侦测器、揭示卡

**消磁仪**：「狗了个狗」的主动道具，只能作用于棋盘中可点击的磁吸方块。目标选择可取消；成功后只移除磁性，保留图案、位置、层叠与其他状态，不进入暂存槽、不触发三消。播放原位消磁动效后，目标恢复普通可点击视觉。
_避免_：消磁卡、磁力清除器

**钥匙**：「狗了个狗」的无目标主动道具，只在用户将其选入当前道具组时可用。初始次数为 0；任意来源的非终局三消在剩余逻辑棋盘单位数大于当前空闲有效槽位时，按阶段配置的 30% 命中率增加 1 次；钥匙次数不超过本局锁槽数。使用钥匙按固定顺序解锁 1 个锁槽，播放开锁动效并扣除 1 次。钥匙次数不写入公共游戏进度，失败、重开、通关后清空。
_避免_：动态奖励道具、钥匙库存

**暂存槽容量提升**：道具使当前局暂存槽总容量与有效容量立即增加 1 格；上锁槽数与钥匙次数上限不变，新有效槽位位于右侧连续锁槽之前。「狗了个狗」每关最多使用一次；通关、失败或重开后失效。
_避免_：永久扩槽、全局容量

**当前道具组**：某个游戏按自身规则确定、用于当前关卡的道具类型选择。「狗了个狗」首次进入时从固定集合中选出 3 种不同道具；选满后需要显式确认，后续关卡默认顺延使用该组。当前道具组不携带已用次数，关卡重新开始时次数按规则初始化。
_避免_：装备栏、用户背包

**道具组配置**：某个游戏按自身规则独立持久保留的道具类型选择。「狗了个狗」保存 3 种不同道具；仅当保存的道具 ID 从固定道具集合中移除时配置失效，需按游戏规则重新选择，不静默替换。某一关暂时没有有效目标或锁槽不改变配置有效性。道具组配置跨离开、刷新与回访保留；不包含道具次数、棋盘或暂存槽等半局状态，重置本地数据时清除。
_避免_：道具库存、局内存档

**道具组变更**：用户可按具体游戏规则替换当前道具组；「狗了个狗」只允许在本关准备态或挑战结束结果页变更，挑战进行中不可变更。变更页默认预选旧组，警告确认只进入编辑态，最终确认新组后才应用新组。对「狗了个狗」，新组至少替换 1 种道具，新旧组完全相同视为取消，不重置准备态棋盘；失败结果页变更用于重试当前关卡并沿用原 `runSeed`，通关结果页变更用于下一关并生成新尝试；变更不回滚已确认的通关或关卡解锁。
_避免_：局内换组、临时库存调整

**撤销**：将已完成的普通选择、三消或道具效果回滚。「狗了个狗」永不支持撤销。
_避免_：回退、悔棋

**可解关卡**：至少存在一条不依赖道具的通关路径的关卡。路径必须模拟锁槽限制、磁吸真实随机目标、双生分裂、冻结自然融化与幻化揭示；不使用钥匙、消磁仪、扩容或其他道具。局部区域中同一图案类型出现数量可以任意；按逻辑方块单位计算时，整关同一图案类型总数仍为 3 的倍数。
_避免_：必胜关、无解关

**安全选择**：玩家当前可执行、且执行后仍保留至少一条无道具通关路径的选择。安全选择数量用于衡量关卡策略空间与难度。
_避免_：正确答案、提示步

**关卡上限**：「狗了个狗」最多开放 99 关。关卡数量有限不代表单局方块数量固定；方块数量从 90 起步，阶段增长至 180 后封顶。所有关卡使用不规则内部逻辑轮廓与完整长方形可见棋盘，后续难度由方块数量、层数、部分重叠布局、安全选择空间与特殊机制密度共同提升。第 99 关通关后展示最终通关页，称号为「你就是最狗的玩家」。
_避免_：随机局、无限棋盘
