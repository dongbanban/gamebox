# 03 — 统一暂存槽规则 helper

**What to build:** 让局内状态、特殊机制和可解性流程共用一份方块入槽转换与复制逻辑，并用标准数组操作完成三消删除，使玩家看到的入槽顺序、特殊机制状态和消除结果保持不变。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] `DogBlock` 到 `DogTrayBlock` 的转换只保留一个共享实现，并由运行时与关卡逻辑共同复用。
- [ ] 暂存槽方块及嵌套特殊机制状态的复制只保留现有共享 clone helper。
- [ ] 删除零消费者的矩形类型和可解性转换 helper，不保留转发别名。
- [ ] 使用标准数组过滤与原地更新替代手写索引删除游标，调用方仍持有同一暂存槽数组。
- [ ] 相邻三消、冻结进度、双生分裂、磁吸入槽、乱序与终局行为保持不变。
- [ ] 使用现有 `GameSession`、特殊机制和可解性测试验证外部结果，不为 helper 结构增加新断言。
- [ ] 运行严格 TypeScript 未使用诊断并记录结果；最终批量 QA 关联 ticket 10。
