# 10 — 完成第三轮精简发布验收

**What to build:** 证明精简后的 Gamebox 仍能完整注册、进入游戏、生成可解关卡、使用道具、完成或失败关卡、保存进度并在受支持浏览器运行，同时记录仓库实际缩减结果。

**Blocked by:** 09 — 清理历史 effort 与一次性工具

**Status:** ready-for-agent

- [ ] 核心测试继续覆盖游戏进度、关卡生成、可解性、难度、局内状态、特殊机制、道具与回放。
- [ ] Worker 准备、同步 fallback、预生成、加载态和失败诊断全部通过。
- [ ] Chromium 浏览器流程覆盖注册、目录、游戏、通关、失败、重试、下一关、返回和刷新。
- [ ] WebKit 与移动 Chromium 覆盖核心流程、响应式布局、键盘操作和减少动态效果。
- [ ] Pages 构建通过，生产入口、Worker、静态资源和 CDN 路径保持可用。
- [ ] `pnpm test:qa` 作为唯一最终顶层验收命令通过，不与 `pnpm test:affected` 叠加。
- [ ] 严格 TypeScript 未使用诊断通过，不存在无效 import、参数、声明或零消费者导出。
- [ ] Git diff 检查和改动文件行数守卫通过。
- [ ] 对比实现前后测试收集，确认 import 迁移和测试删除没有静默丢失外部行为覆盖。
- [ ] 记录实际新增、删除与净删行数；约 5,800 行只是估算，不作为强制目标。
- [ ] 记录依赖变化为 0；如实际出现依赖变化，本 ticket 不得通过，除非另有获批规格。
- [ ] 将最终 QA 命令、结果、测试收集变化和缩减统计回写本 ticket，并关联 tickets 01–09。
