# 06 — 扩展全部无限不规则关卡

**What to build:** 将第 2 关起的无限关卡全部迁移到不规则棋盘与部分重叠生成方案，移除规则形状分支，并让图案种类随进度增长。

**Blocked by:** 05 — 生成首个不规则部分重叠关卡

**Status:** done

- [x] 所有关卡的棋盘形状统一为不规则形，不再生成长方形、五角星形、爱心形或其他规则轮廓。
- [x] 删除规则形状模板、形状阶段池、形状权重与对应渲染分支。
- [x] 第 1–5 关使用 6 种图案，第 6–15 关使用 8 种，第 16 关起使用 10 种。
- [x] 方块数量继续按 90、108、126、144、162、180 阶段增长并封顶。
- [x] 最大层数继续按 3、4、5、6 阶段增长。
- [x] 每关跨层重叠至少 70% 为 `1/4` 或 `1/2`，完全对齐不超过 10%。
- [x] 几何 fallback 继续满足不规则轮廓、部分重叠、同层不重叠与遮挡上限，不能被难度放宽绕过。
- [x] 形状复杂度改为不规则轮廓复杂度与部分重叠分布，不再依赖形状枚举常数。
- [x] 生成器版本提升；同一新版本、关卡号与 seed 仍稳定可重放。
- [x] 固定检查点与压力关卡全部可生成、可加载、可解且不白屏。

## Comments

- 生成器版本从 v3 升至 v4；所有模板与 fallback 仅保留 irregular，动态裁切读取 playable mask。
- 层间偏移固定使用四分之一精度，候选入口统一校验轮廓、同层重叠、遮挡上限与部分重叠比例。
- 验证通过：`pnpm typecheck`、`pnpm test:core`（57 tests）、`DOG_RANDOM_LEVEL_COUNT=1 DOG_STRESS_LEVEL_COUNT=1000 pnpm test:random`（3 tests）、`pnpm build`、`pnpm test:e2e`（12 tests）。
