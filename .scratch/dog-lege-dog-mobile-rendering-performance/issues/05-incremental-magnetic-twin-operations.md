# 05 — 磁吸与双生原子操作保持增量

**What to build:** 玩家选择磁吸或双生方块时，棋盘和暂存槽只增删原子操作真实涉及的节点，同时保留目标选择、分裂、容量压力、三消与输入锁规则。

**Blocked by:** 03 — 三消、锁槽与容量变化保持增量

**Status:** done

- [x] 磁吸方块进入暂存槽时只移除自身棋盘节点，并在存在合法目标时只移除被吸目标节点。
- [x] 磁吸自身与目标按 session 决定的顺序进入暂存槽，未参与操作的棋盘方块和槽位保持原节点。
- [x] 被吸方块保留自身机制并按自身入槽规则处理，幻化揭示、双生分裂及冻结状态不会因增量更新丢失。
- [x] 磁吸吸引和目标飞行动画继续使用操作前捕获的位置，完成前保持输入锁。
- [x] 消磁仪只更新目标磁吸方块的机制属性与静态视觉，不重建全部棋盘。
- [x] 双生方块选择后只移除一个棋盘节点，并在暂存槽中新增两个独立、相邻且稳定 ID 的普通方块节点。
- [x] 双生分裂造成三消、满槽或超容量时，最终节点与失败判定保持现有原子结算语义。
- [x] 磁吸或双生操作触发三消时复用 ticket 03 的增量暂存槽路径，不增加机制专属全量 renderer。
- [x] 通过公开游戏入口覆盖有/无磁吸目标、被吸特殊方块、消磁仪、普通双生及磁吸双生组合。
- [x] 运行 `pnpm test:ui` 并记录结果；本 ticket 不运行完整 QA、affected 或 focused profile。

## Comments

- 2026-09-09：复用现有 session 决策、动画编排及稳定 ID 的棋盘/暂存槽增量同步路径；新增公开 `startDogLegeDogGame` 回归覆盖磁吸有/无目标、冻结/幻化/双生被吸目标、消磁仪、普通双生、磁吸双生、三消、满槽与超容量，并断言节点身份、入槽顺序、输入锁和操作前动画位置。
- 2026-09-09：`pnpm exec vitest run tests/dog-cases/incremental-specials.test.ts` 通过（17 tests）；`pnpm test:ui` 通过（19 files / 114 tests）；`pnpm exec tsc --noEmit --pretty false` 通过；`git diff --check` 通过。按本 ticket 要求未运行完整 QA、`pnpm test:affected` 或 `pnpm test:focused`。
