# 05 — 幻化方块与入槽后显现

**What to build:** 落地只存在于棋盘的幻化方块；它在棋盘上伪装成其他图案，点击后先飞入暂存槽，入槽完成后显现真实图案。

**Blocked by:** 01 — 幻化状态由随机关卡尝试生成；03 — 特殊机制需要共享方块状态与无道具求解规则。

**Status:** done

## V13 迁移状态

本票下方验收保留为历史记录。当前 v13 时序由 ticket 27 覆盖：幻化方块飞行期间保持伪装图案，进入暂存槽完成后才显现真实图案。

- [x] 幻化方块只出现在棋盘，不进入关卡配置之外的暂存槽初始状态；棋盘上显示 mask/幻化动效与伪装图案。
- [x] 每次新尝试随机决定幻化方块位置、真实图案与伪装图案；同一 `runSeed` 下完整稳定，不同尝试允许变化。
- [x] 幻化方块真实图案用于三消、生成验证与解法；伪装图案只用于棋盘视觉，不改变规则。
- [x] 玩家直接点击幻化方块不失败；方块飞入暂存槽期间保持伪装图案且不显现真实图案，入槽完成后播放显现动画。
- [x] 飞入动画期间占用 1 个暂存槽位并锁定棋盘点击；动画结束后真实图案正式进入暂存槽，再执行三消检查。
- [x] 飞行、显现、落槽及三消结算完成后恢复棋盘输入；动画期间不得重复点击或触发并发道具动作。
- [x] 幻化方块无需检测仪也可通过直接点击进入暂存槽并揭示；该行为属于无道具可解路径。
- [x] 幻化方块视觉与普通方块有明确区分，但不增加必须显示的文字说明。
- [x] 增加伪装/真实图案、飞行时序、槽位占用、输入锁、重复点击及无道具求解测试。

## Comments

- 2026-08-20：从 ticket 01 AC5 拆入幻化点击揭示、真实图案参与验证及无检测仪无道具路径验收。依赖 ticket 01 已完成，仍等待 ticket 03 共享特殊机制模型，当前 ticket 保持 `ready-for-agent`。
- 2026-08-21：完成幻化生成、真实/伪装图案状态、棋盘 mask、显现飞行、延迟落槽与三消结算、输入锁及无道具路径；新增 GameSession pending selection seam。
- 2026-08-21：验证通过：`pnpm test:focused`（99 tests）、`pnpm exec vitest run --exclude 'tests/e2e/**' --exclude 'tests/random-regression.test.ts'`（123 tests）、`pnpm test:random`（3 tests）、`pnpm test:e2e`（18 tests）、`pnpm build`、`git diff --check`。
- 2026-08-21：`pnpm test:qa` 的 `test:core` 仍会错误收集 Playwright spec；按仓库 AGENTS 说明改用排除 E2E 的 core 命令，结果通过。代码审查发现的“动画前已提交幻化落槽”时序问题已改为动画完成后提交。
