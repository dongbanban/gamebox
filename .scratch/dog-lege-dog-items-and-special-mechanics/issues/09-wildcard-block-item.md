# 09 — 万能方块

**What to build:** 落地万能方块道具；玩家点击暂存槽内已有方块后，万能方块复制其图案进入暂存槽，并优先帮助冻结方块完成三消。

**Blocked by:** 03 — 需要冻结方块优先级与状态规则；04 — 需要统一道具运行时与目标确认。

**Status:** done

- [x] 使用万能方块后进入槽内目标选择；只能点击当前暂存槽中已有且存在合法使用计划的图案方块，不单独列出本关图案；可重复选择同一图案，没有合法槽内目标时不可用。
- [x] 最终确认前可以取消；取消不改变棋盘、暂存槽或次数；确认后原子完成图案转换、入槽及棋盘补偿。
- [x] 万能方块进入暂存槽后显示高亮边框或等效视觉标记，与普通同图案方块明确区分。
- [x] 使用时从棋盘不可点击区域减少 1 个所选同图案方块；无可合法减少的方块、会破坏无道具可解性或当前状态不允许时，使用失败且不扣次数。
- [x] 万能方块严格追加到暂存槽末尾；仅追加前槽尾两个连续同图案方块可与万能组成三消，其中冻结方块可优先作为本次连续三消参与者，不跨过其他槽位重排。
- [x] 支持已确认槽尾场景：2 个冻结 + 万能完成三消；1 个冻结 + 1 个普通 + 万能完成三消；未参与三消的普通或冻结方块保留。
- [x] 万能方块只帮助本次三消，不永久融化棋盘中其他冻结方块；三消完成后按现有冻结规则继续。
- [x] 成功使用扣 1 次；道具不会造成失败，执行与三消动画期间锁定输入。
- [x] 增加槽内目标选择、空槽禁用、取消、棋盘补偿、可解性校验、冻结优先级、视觉标记、次数及原子性测试。

## Comments

- 2026-08-24：完成 `GameSession` 万能方块计划与原子结算；只补偿不可点击同图案棋盘方块，逐候选验证道具后无道具可解性。万能严格尾追加，仅与原槽尾连续同款 pair 完成三消；pair 可含冻结，非后缀同款不跨位重排。
- 2026-08-24：完成槽内目标选择/取消、重复选择、延迟提交失败返还次数、动画输入锁、三消反馈锁及万能 `visualMarker` 高亮边框；同图案普通方块与万能方块保持可观察区分。
- 2026-08-24：双轴 code review 修复实时棋盘机制状态未进入可解性投影、冻结/幻化补偿边界、非后缀万能三消、共享 wildcard payload、重复 effect 映射与失败结果伪 ID；失败结果改为 `used` 判别 union。Standards/Spec 最终复审无 finding。
- 2026-08-24：最终验证通过：`pnpm test:focused`（141 tests）、`pnpm test:ui`（47 tests）、`pnpm exec vitest run --exclude 'tests/e2e/**' --exclude tests/random-regression.test.ts --reporter dot`（165 tests）、`pnpm test:random`（3 tests，含 1–100 关前缀与 100 关压力测试）、`pnpm test:e2e`（19/19）、`pnpm typecheck`、`pnpm build`、`git diff --check`。
- 2026-08-24：按交互反馈收口万能目标：只暴露存在合法计划的暂存槽方块，点击槽块复制其图案；空槽或无合法槽内目标时禁用；删除独立图案按钮列表。
- 2026-08-24：槽内目标调整验证通过：`pnpm test:focused`（143 tests）、`pnpm test:ui`（47 tests）、`pnpm exec vitest run --exclude 'tests/e2e/**' --exclude tests/random-regression.test.ts --reporter dot`（167 tests）、`pnpm test:random`（3 tests，含 1–100 关前缀与 100–1000 关压力测试）、`pnpm test:e2e`（19/19）、`pnpm typecheck`、`pnpm build`、`git diff --check`。
