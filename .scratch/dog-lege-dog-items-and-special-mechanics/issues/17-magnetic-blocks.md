# 17 — 磁吸方块完整入槽链路

**What to build:** 落地磁吸方块，让点击一次可复现地吸取一个符合规则的棋盘方块，并在同一原子操作中完成目标机制、三消与胜负结算。

**Blocked by:** 10 — 多特殊机制生成、求解与难度 seam；14 — 磁吸数量曲线与随机配置；16 — 被吸双生方块必须沿用双生分裂规则。

**Status:** done

- [x] 磁吸方块进入暂存槽时先占用自身位置，再吸取至多 1 个目标；目标候选排除所有磁吸方块与真实图案相同的方块。
- [x] 目标选择优先可点击的非磁吸不同真实图案方块；没有可点击候选时才从不可点击候选中选择；没有合法候选时磁吸方块独自入槽。
- [x] 磁吸不可吸引磁吸，不产生连锁；被吸方块保留冻结、幻化、双生或后续机制，并按自身入槽规则与动效处理。
- [x] 入槽顺序固定为磁吸源先入槽、目标后入槽；目标机制完成后统一处理所有三消与胜负，中间状态不判失败。
- [x] 磁吸完成一次吸引后消耗磁性；暂存槽中的源方块按普通方块参与后续三消。
- [x] 目标选择由 `runSeed` 派生的独立磁吸随机流驱动；相同 seed 与操作路径复现相同目标，不使用有利结果替换规则。
- [x] 磁吸源与持续存在的磁吸状态显示静态识别动效；点击后播放指向目标、目标飞入与状态消耗动效。
- [x] 因磁吸最终导致满槽无三消或超容量而失败，视为玩家操作结果；不做预禁用、自动改目标或其他安全控制。
- [x] 增加候选过滤、可点击优先、不可点击回退、无目标、禁止连锁、目标自身机制、原子结算、seed 回放、失败语义与视觉时序测试。

## Comments

- 2026-08-25：接收 Ticket 14 原 AC 中磁吸数量配置的运行时接入、独立磁吸随机流、目标机制与磁吸失败边界；跨机制整关回放与密度/求解收口由 20 验收。
- 2026-08-25：完成磁吸源/目标原子入槽、可点击优先与不可点击回退、同图案/磁吸过滤、冻结/幻化/双生目标机制、磁性消耗、独立 `runSeed` 流、最终容量失败语义与控制器时序；生成器、求解器、完整跨机制回放仍由 Ticket 20 接入。
- 2026-08-25：验证通过 `pnpm exec vitest run tests/special-mechanism.test.ts --reporter dot`（42/42）、`pnpm test:focused`（9 files/172 tests）、`pnpm exec vitest run --exclude 'tests/e2e/**' --exclude tests/random-regression.test.ts --reporter dot`（197/197）、`pnpm test:random`（3/3）、`pnpm test:e2e`（19/19）、`pnpm test:e2e:cross-browser`（9/9）、`pnpm build`、`pnpm typecheck`、`git diff --check`。
