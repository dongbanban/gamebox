# 15 — 7 种道具集合与换组生命周期

**What to build:** 将「狗了个狗」固定道具集合扩展为 7 种，新增消磁仪与钥匙，并让准备态/结果页换组规则与已确认 spec 一致。

**Blocked by:** None — 02、04、13 已完成，可直接开始。

**Status:** done

- [x] 道具浮层展示 7 种固定道具：道具三消移除、暂存槽容量提升、万能方块、火把、检测仪、消磁仪、钥匙；用户仍需选择恰好 3 种不同道具。
- [x] 消磁仪与钥匙作为普通道具注册并显示独立图标、说明与次数；钥匙已选入时初始次数为 0，不因未选入或本关暂时没有锁槽而动态加入。
- [x] 锁槽数为 0 时钥匙仍可选择与展示，但本局不掉落钥匙次数、不允许有效解锁。
- [x] 道具组只在当前关卡尚未开始或挑战结束后可更换；挑战进行中、目标选择、道具动画、结算与结果确认期间禁止更换。
- [x] 失败结果页换组后重试当前关卡并沿用原棋盘与 `runSeed`；通关结果页换组只作用于下一关并生成新尝试；已确认通关、奖励与解锁不回滚。
- [x] 道具组持久化只保存 3 个道具 ID；刷新、离开与回访保留选择，不保存次数、钥匙数量、棋盘或暂存槽。
- [x] 增加 7 选 3、钥匙初始 0、锁槽数为 0、准备态/结果页换组、失败复用 seed、通关进入下一关与持久化边界测试。

## Comments

- 2026-08-25：固定集合扩展至 7 项，新增消磁仪/钥匙图标、定义、次数展示与 runtime 注册；钥匙保持 0 次且无锁槽时不可用。挑战开始后锁定换组，失败复用原 `runSeed`，胜利进入下一关并生成新尝试；持久化严格保存 Dog 3 个 ID，通用 ProgressStore 仍保留 opaque loadout 边界。
- 验证：`pnpm test:focused`（9 files、154 tests）；`pnpm exec vitest run --exclude 'tests/e2e/**' --exclude tests/random-regression.test.ts`（11 files、178 tests）；`pnpm test:random`（3/3）；`pnpm test:e2e`（19/19）；`pnpm build`；`git diff --check`。
- 消磁仪完整磁吸效果、钥匙锁槽/掉落/解锁效果留在后续 ticket；本 ticket 完成固定集合、零次数语义与生命周期边界。
